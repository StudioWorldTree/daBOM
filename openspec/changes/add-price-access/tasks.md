# Tasks

- [ ] Quote columns: method (`api` | `crawl` | `headed` | `seed`), url, as-of (checkedAt already exists — keep or alias)
- [ ] OpenAPI Quote create/patch include method and url
- [ ] POST /quotes does not delete prior quotes for that item
- [ ] Document ladder in OpenAPI description / well-known spec
- [ ] Tests: two quotes on one SKU both persist; preferred still drives roll-up
